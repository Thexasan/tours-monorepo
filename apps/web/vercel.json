{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install --include=dev",
  "buildCommand": "cd ../../packages/types && npm run build && cd ../db && npx prisma generate && npm run build && cd ../../apps/web && next build"
}
